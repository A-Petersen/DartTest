name: DartTest
description: An absolute bare-bones web app.
version: 0.0.1
#homepage: https://a-petersen.github.io/DartTest/
#author: Arne Petersen und Daniel Figia

environment:
  sdk: '>=1.20.1 <2.0.0'

dependencies:

dev_dependencies:
  browser: ^0.10.0
  dart_to_js_script_rewriter: ^1.0.1
  key_listener: ^1.0.0

transformers:
- dart_to_js_script_rewriter

# Uncomment the following in sdk 1.24+ to make pub serve
# use dartdevc (webdev.dartlang.org/tools/dartdevc).
#web:
#  compiler:
#    debug: dartdevc
